import { normalizedComments } from '../components/data';
import {  } from '../../AC/constants';
// import moment from 'moment';

export default (commentsState = normalizedComments, action: any) => {
    switch (action.type) {
        
    }

    return commentsState;
}